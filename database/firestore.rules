rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function signedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return signedIn() &&
        exists(/databases/$(database)/documents/admin_users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/admin_users/$(request.auth.uid)).data.active == true;
    }

    function isRole(role) {
      return isAdmin() &&
        get(/databases/$(database)/documents/admin_users/$(request.auth.uid)).data.role == role;
    }

    function hasAnyRole(roles) {
      return isAdmin() &&
        get(/databases/$(database)/documents/admin_users/$(request.auth.uid)).data.role in roles;
    }

    function isSelf(uid) {
      return signedIn() && request.auth.uid == uid;
    }

    // Default deny. Every collection must have an explicit rule.
    match /{document=**} {
      allow read, write: if false;
    }

    // Admin directory.
    // Bootstrap these documents manually from Firebase Console
    // or trusted server tooling. Clients cannot create/modify
    // their own admin access.
    match /admin_users/{uid} {
      allow read: if isSelf(uid);
      allow write: if false;
    }

    // User profile.
    // User can read/update only their own non-privileged profile.
    match /users/{uid} {
      allow read: if isSelf(uid) ||
        hasAnyRole(['SUPER_ADMIN', 'SUPPORT', 'EVENT_ADMIN', 'FINANCE', 'AUDITOR']);

      allow create: if isSelf(uid);

      allow update: if isSelf(uid)
        && request.resource.data.role == resource.data.role;

      allow delete: if false;
    }

    // Events.
    match /events/{eventId} {
      allow read: if signedIn();
      allow create, update, delete: if isRole('SUPER_ADMIN');
    }

    // =========================================================
    // PAYMENT INTENTS
    // =========================================================
    //
    // Phase 21 frontend currently creates Payment Intent directly
    // from the authenticated admin client.
    //
    // Initial client-created status:
    // REQUIRES_PAYMENT
    //
    // created_by must equal the authenticated admin UID.
    //
    // Payment status must NOT be changed by the client.
    // PAID/SUCCEEDED and ticket activation remain trusted-backend
    // operations.
    match /payment_intents/{intentId} {

      // Admin/authorized finance/event roles can read intents.
      // The creator can also read the intent.
      allow read: if signedIn() &&
        (
          hasAnyRole([
            'SUPER_ADMIN',
            'FINANCE',
            'EVENT_ADMIN',
            'AUDITOR'
          ]) ||
          resource.data.created_by == request.auth.uid
        );

      // Authorized admin roles may create a Payment Intent.
      //
      // The browser must identify the authenticated admin as
      // created_by and the initial state must be
      // REQUIRES_PAYMENT.
      allow create: if signedIn() &&
        hasAnyRole([
          'SUPER_ADMIN',
          'FINANCE',
          'EVENT_ADMIN'
        ]) &&
        request.resource.data.created_by == request.auth.uid &&
        request.resource.data.status == 'REQUIRES_PAYMENT';

      // Client cannot modify or delete a Payment Intent.
      // Payment state changes belong to trusted backend/webhook flow.
      allow update, delete: if false;
    }

    // Orders.
    match /orders/{orderId} {
      allow read: if signedIn() &&
        (resource.data.user_id == request.auth.uid ||
         hasAnyRole(['SUPER_ADMIN', 'FINANCE', 'EVENT_ADMIN', 'SUPPORT', 'AUDITOR']));

      allow create: if signedIn() &&
        request.resource.data.user_id == request.auth.uid;

      allow update, delete: if false;
    }

    // Payment ledger: read only for authorized users. No client writes.
    match /payment_ledger/{ledgerId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'FINANCE', 'EVENT_ADMIN', 'AUDITOR']) ||
        (signedIn() && resource.data.user_id == request.auth.uid);

      allow write: if false;
    }

    // Provider/webhook events: never client-writable.
    match /webhook_events/{webhookId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'FINANCE', 'AUDITOR']);
      allow write: if false;
    }

    // Verification results: never client-writable.
    match /payment_verifications/{verificationId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'FINANCE', 'EVENT_ADMIN', 'AUDITOR']) ||
        (signedIn() && resource.data.user_id == request.auth.uid);

      allow write: if false;
    }

    // Tickets: client may read their own ticket.
    // Creation/status changes are trusted backend only.
    match /tickets/{ticketId} {
      allow read: if signedIn() &&
        (resource.data.user_id == request.auth.uid ||
         hasAnyRole(['SUPER_ADMIN', 'EVENT_ADMIN', 'SUPPORT', 'AUDITOR']));

      allow create, update, delete: if false;
    }

    // Reconciliation.
    match /reconciliation/{recordId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'FINANCE', 'EVENT_ADMIN', 'AUDITOR']);
      allow write: if false;
    }

    // Audit logs are immutable to clients.
    match /audit_logs/{auditId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'AUDITOR', 'FINANCE']);
      allow write: if false;
    }

    // Sandbox is intentionally isolated from production.
    match /sandbox_runs/{runId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'FINANCE', 'EVENT_ADMIN', 'AUDITOR']);
      allow create: if hasAnyRole(['SUPER_ADMIN', 'EVENT_ADMIN']);
      allow update, delete: if false;
    }

    // Failure tests are non-production and cannot affect payment/tickets.
    match /failure_tests/{testId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'AUDITOR', 'FINANCE']);
      allow create: if hasAnyRole(['SUPER_ADMIN', 'EVENT_ADMIN']);
      allow update, delete: if false;
    }

    // Operational health checks.
    match /health_checks/{checkId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'AUDITOR', 'FINANCE']);
      allow create: if hasAnyRole(['SUPER_ADMIN', 'EVENT_ADMIN']);
      allow update, delete: if false;
    }

    // Final audit records are evidence only;
    // they cannot grant production approval.
    match /final_audits/{auditId} {
      allow read: if hasAnyRole(['SUPER_ADMIN', 'AUDITOR', 'FINANCE']);
      allow create: if hasAnyRole(['SUPER_ADMIN', 'AUDITOR']);
      allow update, delete: if false;
    }

    // Any future collections remain denied until explicitly reviewed.
  }
}
