BEEPAY 23.5.7 — CATATAN UPLOAD / DEPLOY
========================================
Basis:
- beepay-main-23.5.6-concurrency-diagnostic-hardening(1).zip
- Seluruh struktur folder dipertahankan.

PERUBAHAN UTAMA
1. backend/Code.gs
   - Versi backend: 23.5.7.
   - Memperbaiki error Provider Adapter Security Test:
     "testStartedAtMs is not defined".
   - Menambahkan timestamp awal test pada processSandboxProviderAdapterTest().
   - Tidak mengubah boundary SANDBOX/PJP live.
   - Global Script Lock tetap bounded 5 detik dan fail-closed saat sibuk.

2. js/app.js
   - Versi frontend: 23.5.7.
   - getIdToken(true) diubah menjadi getIdToken(false) agar browser tidak memaksa
     refresh token pada setiap request. Firebase SDK tetap melakukan refresh saat
     memang diperlukan.
   - Read cache + in-flight deduplication tetap dipakai.
   - Firestore list read diberi batas waktu lokal 30 detik agar request UI yang
     macet tidak menumpuk sebagai pekerjaan browser.
   - Tidak menambahkan realtime Firestore listener.

3. js/app.optimized.js
   - Perubahan yang sama dengan js/app.js karena file ini yang dipakai index.html.
   - Dipastikan tetap sinkron dengan source utama.

4. index.html
   - Versi UI/footer/title diperbarui ke 23.5.7.
   - Tetap menyatakan bahwa admin console tidak membuat realtime Firestore listener.

5. File lain
   - config/config.js TIDAK diubah.
   - database/firestore.rules TIDAK diubah.
   - database/firestore-schema.json TIDAK diubah.
   - css/style.css TIDAK diubah.
   - vercel.json TIDAK diubah.
   - firebase-config.js TIDAK diubah.

FILE YANG DIUPLOAD
Frontend / GitHub / Vercel:
- index.html
- js/app.js
- js/app.optimized.js
- CATATAN-UPLOAD-BEEPAY-23.5.7.txt (opsional, dokumentasi)

Backend / Google Apps Script:
- backend/Code.gs
- backend/appsscript.json TIDAK berubah, tetapi tetap termasuk dalam ZIP utuh.

ZIP UTUH
- Seluruh isi beepay-main disertakan, bukan patch-only.

CATATAN TRAFFIC / LISTENER
- Tidak ada onSnapshot(), onValue(), atau Firestore realtime listener baru.
- Admin login hanya memasang satu onAuthStateChanged listener per tab melalui guard.
- List read menggunakan cache 15 detik dan deduplikasi in-flight per key.
- Automatic post-login list loading tetap disabled.
- Backend write/test kritis tetap melalui trusted Apps Script + Script Lock.
- Browser tidak diberi kewenangan menetapkan payment menjadi trusted PAID.

HASIL VALIDASI LOKAL
- JavaScript syntax: PASS.
- Code.gs syntax check via Node parser: PASS.
- Tidak ditemukan onSnapshot/onValue/addSnapshotListener pada JS.
- Tidak ada perubahan pada Firebase Rules pada versi ini.

URUTAN UPLOAD
1. Upload backend/Code.gs ke Google Apps Script dan Deploy versi baru.
2. Pastikan URL /exec yang dipakai config tetap menunjuk deployment aktif.
3. Upload index.html, js/app.js, js/app.optimized.js ke GitHub.
4. config/config.js tidak perlu diubah.
5. Deploy Vercel.
6. Hard refresh browser.
7. Login.
8. Jalankan Provider Adapter Security Test.
9. Lanjutkan Concurrency Test dan Mismatch Concurrency Test.

JANGAN MENGUPLOAD PATCH SAJA.
Gunakan ZIP UTUH 23.5.7 sebagai source of truth.
